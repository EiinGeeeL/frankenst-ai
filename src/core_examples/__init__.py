"""Repository reference package built on top of `frank`.

`core_examples` contains the concrete layouts, models, prompt assets,
configuration files and utilities used to demonstrate how the reusable
`frank` contracts are applied in a real LangGraph project.

It is intentionally treated as the reference package of this mono-repo, not as
the stable public API of the published base `frank` wheel.
"""
